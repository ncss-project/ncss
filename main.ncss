#main {
    /* this is comment */
    --num: 4;
    transform: --num 5;
    content: "--num is " + var(--num);

    .if[--num >= 5] {
        content: '--num 5 or higher.';
    }

    .else {
        content: "--num is less than 5.";
    }
}